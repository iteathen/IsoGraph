# Current Integrated Stack with DTS — Qualification Plan

**Status:** frozen qualification design; no semantic call authorized yet  
**Experiment:** 027  
**Scope:** qualified Core 0.17 + Core 0.18 + QU 0.1 + NEI 0.4 + DP 0.1–0.6 + DTS 0.1

## Objective

Test the expanded qualified-module composition after DTS 0.1 promotion.

This campaign tests whether new unsound inference paths appear when qualified DTS transition semantics are composed with the already-qualified current stack. It does not requalify the individual modules.

## Fresh semantic burden

The holdout must use new cases not replayed from Experiments 019 or 026 and must exercise, at minimum:

1. determinate transition observation without artificial QU or known mechanism;
2. unresolved load-bearing transition structure requiring QU;
3. endpoint equality failing to establish TI when load-bearing transition structure differs;
4. valid scoped TI with projected mechanism/profile differences retained as residual;
5. QU-bearing TI without equality of actual realizations or NEI SAME;
6. projected decomposition differences not creating false TI rejection;
7. load-bearing decomposition/order mismatch rejecting TI;
8. exact NEI identity not implying TI;
9. DP structural discovery not supplying TI proof authority;
10. target-driven projection/QU restriction remaining circular;
11. missing QU not masquerading as completeness;
12. observation-first quantity alignment preserving qualification/discovery separation;
13. QU refinement changing a later TI result without rewriting historical evidence;
14. mechanism/profile classification not supplying natural identity or base DTS truth;
15. partial transition common core retaining load-bearing residuals;
16. DTS reusing ordinary boundary/interface semantics rather than inventing a second boundary substrate;
17. role-count/factorization similarity not proving invariant structure or TI;
18. scoped TI coexisting with a broader unresolved natural-identity question.

## Evidence split

Before any external semantic call:

- freeze the qualified-stack authority packet;
- freeze the 18 public cases and complete serializer contract;
- freeze hidden proposition-specific assertions;
- implement deterministic exact scoring;
- verify a perfect synthetic report passes;
- verify a deliberately wrong report fails;
- verify cold packet isolation;
- verify exact qualified semantic hashes used by the packet;
- run normal repository Verify.

Then perform one cold decoder call over the complete frozen packet.

No hidden answer key, prior Experiment 019 output, prior Experiment 026 output, author audit, scorer implementation, repository routing docs, or qualification result may enter the decoder packet.

## Provider shape

Use the proven cold request shape:

```text
model: gemini-3.1-flash-lite
maxOutputTokens: 8192
temperature: 0.1
thinkingConfig: omitted
```

Provider failure before a frozen semantic report is `INFRASTRUCTURE_FAILURE`, not semantic failure.

## Promotion gate

The expanded composition may be recorded as integration-qualified only when:

- all 18 fresh cases pass;
- deterministic scoring guards pass;
- module assessment is `SUPPORTED`;
- no hidden serializer contract affects the score;
- DTS/QU/NEI/DP authority boundaries remain intact;
- no profile result substitutes for DTS base semantics;
- historical Experiment 019 and 026 dispositions remain unchanged.

A failing frozen semantic report is preserved unchanged and classified by failure owner before any repair.
