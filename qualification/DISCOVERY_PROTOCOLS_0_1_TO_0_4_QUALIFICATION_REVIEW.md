# Discovery Protocols 0.1-0.4 — Cumulative Qualification Review

**Disposition:** QUALIFIED as the current cumulative Discovery Protocol module  
**Qualified semantic baseline:** Core 0.17  
**Identity dependency:** NEI only when a discovery conclusion actually relies on natural identity/distinctness

## Evidence chain

### Experiment 007

Strict cold semantic discovery recovered 5/5 hidden motifs and 3/3 deep motifs. Its frozen automatic disposition remains `DOES_NOT_QUALIFY` because the hidden scorer required exact rejection tokens not required by the public prompt. This run remains historical evidence and is not rescored.

### Experiment 008

Fresh post-QRC blind holdout formally `QUALIFIES`: 5/5 motifs, 3/3 deep motifs, all guards pass, residuals preserved, falsification recorded, overclaims rejected, promotion bounded to `SUPPORTED_CANDIDATE`.

### Experiment 010

Fresh cumulative DP holdout remains formally `DOES_NOT_QUALIFY`. Six of eight cases passed. Post-freeze review identified:

- D06: hidden oracle wrongly excluded a legal surviving `CORRESPONDENCE` state;
- D08: genuine omission of explicit `same algorithm` rejection despite complete deep topology recovery.

### Experiment 012

Fresh focused regression discharged the D06 semantic issue and recovered a new deep termination topology with all four overclaims explicitly rejected. Its frozen automatic disposition remains `DOES_NOT_QUALIFY` solely because the hidden scorer required singleton mapping endpoints to be arrays while the public prompt had not specified that wire shape.

### Experiment 014

Fresh corrected holdout published the exact array-valued mapping contract before freeze and used a new finite-resource topology.

Attempt 1 was provider-only HTTP 503 with no semantic report. Attempt 2 used the same frozen SHA/packet and formally scored:

- `QUALIFIES`
- QRC semantic rejection coverage: true
- mismatches: none.

## Obligations discharged

The cumulative record now supplies fresh isolated evidence that the module:

- searches structure before lexical coincidence;
- preserves correspondence despite misleading labels;
- rejects a candidate only on a concrete failed structural obligation, rigid authority, or qualified identity separator as applicable;
- does not treat different machinery/factorization depth as a structural breaker;
- promotes or expands cross-residual correspondences instead of discarding them;
- performs one-node-to-subgraph alignment where decomposition granularity differs;
- permits supported `CORRESPONDENCE`, bounded `DORMANT`, or `INCOMPLETE` states where no separator exists;
- preserves method-specific residuals;
- explicitly rejects full-proof isomorphism, natural identity, same intermediate objects, and same algorithm when unsupported;
- keeps discovery priority distinct from semantic authority.

## Qualification conclusion

Discovery Protocols 0.1 through 0.4 are qualified as one cumulative current module at their tested revisions.

Promotion changes qualification status/provenance only. It adds no Core primitive, no new relation kind, no mandatory exhaustive search, no identity inference rule, and no universal discovery-completeness claim.

Historical experiment dispositions remain immutable and are interpreted at their exact frozen contracts.
