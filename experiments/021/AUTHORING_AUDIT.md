# Experiment 021 — Authoring Audit

**Status:** deterministic pre-trigger review

## Frozen semantic target

Unchanged across provider retries:
- DTS 0.1 candidate;
- transition corpus;
- comparison views;
- hidden required claims;
- deterministic scorer.

## Focused transport packet

After two provider-only HTTP 503 runs, the cold transport packet was reduced under obligation-sufficiency discipline.

It now contains:
- `experiments/021/FOCUSED_AUTHORITY.md`, which pins the exact qualified Core 0.18 / QU 0.1 / DP hashes and restates only clauses load-bearing for this development campaign;
- the full DTS 0.1 candidate;
- frozen transition corpus;
- frozen comparison views;
- baseline authority statement;
- public output prompt.

The focused authority file does **not** replace any qualified module and contains no expected transition pair map.

The packet excludes:
- hidden assertions;
- scorer and scorer self-test;
- expected pair map;
- prior Experiment 021 output/evidence;
- this authoring audit;
- repository routing/agent guidance.

## Hidden coverage

The hidden scorer requires eight high-information controls spanning:
- positive scoped TI;
- view-sensitive rejection;
- alternate-decomposition TI under effect view;
- decomposition-sensitive rejection;
- false same-domain analogy rejection;
- QU-bearing scoped TI with residual;
- QU-bearing incompleteness.

Additional discoveries are permitted.

The scorer self-test accepts a perfect synthetic report and rejects an incorrect full-process TI for T04A/T04B.

## Provider history

Runs `36183338156` and `36183579627` produced only HTTP 503 provider failures, no semantic report, and no score. Their evidence remains preserved.
