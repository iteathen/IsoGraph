# Experiment 015 — Authoring Audit

- Fresh P/Q topology replaces Experiment 011's A/B case.
- The public output contract has dedicated booleans for full/global isomorphism, natural identity, same intermediate objects, and same algorithm.
- No free-text normalization is qualification-bearing for those four claims.
- Check(Q2,Q3, policy_tag) creates the extra mapped-target relation that blocks strong embedding.
- Q5/Q6 is an unmapped Beta residual and Q5's `audit` label is a trap.
- u/v share the same open topology and constraints; no NEI authority is supplied.
- P1 atomic source provenance remains source-local D, not a pair residual.
- Scorer tests fail on same-algorithm overclaim, strong-embedding overclaim, and label-trap mapping.
- No prior integrated output is cold-visible.
