# Experiment 028 — Authoring Audit

**Status:** PASS BEFORE EXTERNAL SEMANTIC EXECUTION

## Scope audit

DP 0.7 section 17 contains seventeen qualification targets.

Experiment 028 public cases freshly cover:

- D01 -> target 1
- D02 -> targets 2 and 12
- D03 -> targets 3 and 9
- D04 -> target 4
- D05 -> target 5
- D06 -> target 6
- D07 -> target 7
- D08 -> target 8
- D09 -> target 10
- D10 -> target 13
- D11 -> target 14
- D12 -> target 15
- D13 -> target 16

Fresh predecessor evidence from the completed three-positive-control campaign covers:

- target 11 — primitive-first isomorph discovery;
- target 17 — exact-witness support closure.

No section 17 target is left without identified evidence.

## Negative-control discipline

The holdout does not use arbitrary real-world negative pairs.

Negative outcomes are local and falsifiable against represented distinctions:

- D02 rejects one label-driven candidate mapping because the primitive occurrence semantics differ;
- D08 refuses to infer recursion from a cycle without recursion authority;
- D10 rejects one exact higher-order candidate because a specific primitive boundary incidence fails;
- D12 rejects exact DTS correspondence because a pinned load-bearing order relation differs.

None of these cases authorizes a global claim that no other isomorphism can exist.

## Dependency audit

The cases invoke qualified dependencies only when load-bearing:

- QU: D05, D06, D07, D11;
- DTS: D12;
- NEI: D13;
- Core/DP structural boundaries: all cases as applicable.

The cold packet pins current qualified dependency revisions and the exact current DP 0.7 candidate hash.

## Isolation audit

Cold-permitted files are exactly:

1. Core 0.18;
2. QU 0.1;
3. NEI 0.4;
4. DTS 0.1;
5. DP 0.7 candidate;
6. Experiment 028 baseline authority;
7. Experiment 028 public cases;
8. Experiment 028 public prompt.

Excluded from the cold packet:

- hidden assertions;
- scorer and scorer tests;
- this author audit;
- prior Experiment 028 evidence;
- repository status/routing files;
- positive-control hidden oracles/scoring files;
- prior decoder outputs.

## Serializer/scorer audit

The public prompt specifies all keys and enum alternatives without exposing expected values.

Hidden assertions specify one exact expected answer object per case.

Deterministic scorer tests verify:

- a perfect synthetic report qualifies;
- one wrong semantic answer is rejected;
- one failed self-audit is rejected;
- duplicate case IDs are rejected.

Runner dry-run tests verify:

- exact cold packet membership and order;
- forbidden inputs are absent;
- current DP 0.7 hash is pinned;
- thirteen cases are present;
- the fresh positive-control predecessor is pinned for targets 11 and 17.

## Resource audit

External semantic budget for the frozen candidate is one batched decoder invocation covering D01-D13.

Provider retries/failover are infrastructure handling, not additional semantic sampling for a more favorable result.

No second semantic verifier is planned unless the frozen result exposes an ambiguity that deterministic scoring cannot adjudicate.
