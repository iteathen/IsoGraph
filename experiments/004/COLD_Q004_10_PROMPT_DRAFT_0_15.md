# IsoGraph Experiment 004 — Q004-10 Cold Extraction-Freeze Prompt — Draft 0.15

You are an isolated IsoGraph structural comparison auditor.

Read only:
1. `CORE_SPEC_DRAFT_0_15_CONSOLIDATED_CANDIDATE.md`
2. `experiments/003/STRUCTURAL_COMPARISON_PROTOCOL_006.md`
3. `experiments/004/Q004_10_EXTRACTION_FREEZE_004.axh`
4. this prompt

Do not read assertions, results, reviews, author audits, source amendment specs, registries/catalogs, or gloss.

If historical protocol wording conflicts with consolidated Draft 0.15 semantics, the consolidated specification controls.

For this frozen synthetic fixture:

```text
(^4950 SOURCE SOURCE_BODY)
(^4951 EXTRACTION SOURCE BODY) = independently frozen pre-pair extraction
(^4952 EXTRACTION SOURCE BODY) = pair-conditioned exploratory extraction created after the prospective analogy was visible
```

The marker distinction is test provenance; it does not alter raw component structure.

## Task

1. Reproduce sources `1000` and `2000` and all extraction records.
2. Compare frozen extraction `1100` with frozen extraction `2100` under V0.
3. Compare `1100` with exploratory extraction `2200` under V0 and give a mapping if one exists.
4. State whether an isomorphism that appears only after pair-conditioned `^4952` extraction may count as blind qualification evidence for the original discovery run.
5. State the strongest honest result about the pair under the frozen pre-pair extraction set.
6. State how the post-hoc isomorphism may be retained as exploratory evidence without rewriting the frozen qualification result.

Do not replace the frozen extraction set merely because another component gives a cleaner match. Freeze the result before consulting anything else.
