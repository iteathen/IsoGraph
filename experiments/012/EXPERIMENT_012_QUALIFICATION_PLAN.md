# Experiment 012 — Focused Discovery Protocol Qualification Plan

**Status:** development candidate until frozen  
**Target:** unresolved cumulative DP 0.1-0.4 promotion obligations after Experiment 010.

## Why this experiment exists

Experiment 010 remains formally `DOES_NOT_QUALIFY`.

Its post-freeze diagnosis separated two issues:

1. D06 exposed an overly restrictive hidden oracle: the cold report legally retained a supported `CORRESPONDENCE`, while the scorer allowed only `DORMANT|INCOMPLETE`.
2. D08 recovered the full intended deep topology and residual but omitted explicit rejection of the public `SAME_ALGORITHM` overclaim.

Experiment 012 does not rewrite or replay Experiment 010. It uses a new focused holdout to test only those unresolved burdens.

## Cold packet

Permitted:

- Core 0.17;
- NEI 0.1 and 0.2 for identity-separator discipline;
- Discovery Protocols 0.1-0.4;
- Experiment 012 public cases;
- Experiment 012 public prompt.

Excluded:

- Experiment 010 output/evidence;
- hidden assertions/scorer;
- prior Experiment 012 output;
- author audit;
- repository map/agent guidance;
- browsing.

## Qualification targets

R01 tests that a structurally supported candidate with no separator may remain `CORRESPONDENCE`, stop `DORMANT`, or remain `INCOMPLETE`, but may not be rejected by relabeling representational differences.

R02 is a new topology. It must recover:

- relation/step role;
- finite ranking/well-founded bound;
- strict descent;
- infinite-descent contradiction;
- impossibility;
- termination;

while attempting the compressed-to-decomposed ranking alignment and preserving the Beta-specific layer-derivation mechanism as residual.

It must explicitly reject all four public QRC overclaims, including `SAME_ALGORITHM`.

## Promotion interpretation

A pass does not erase Experiment 010. Promotion review must interpret the evidence cumulatively:

- Experiments 007/008: prior blind structural-discovery success;
- Experiment 010: six direct passes, one scorer-contract defect, one explicit-overclaim omission;
- Experiment 012: focused fresh evidence for the unresolved obligations.

## Resource budget

One cold Gemini decoder invocation plus deterministic scoring. An additional verifier is used only if the cumulative promotion review requires independent semantic adjudication.
