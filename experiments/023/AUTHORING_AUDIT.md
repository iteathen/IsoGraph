# Experiment 023 — Authoring Audit

**Status:** deterministic pre-trigger review

The structural corpus is regenerated mechanically from the exact normative Section 0 barrier blocks of six source modules.

Cold inputs:
- focused qualified discovery authority;
- source-faithful barrier corpus with lexical/provenance labels;
- native `.isg` graph with six distinct source-local relation labels;
- public experiment README;
- public output prompt.

Excluded from the cold packet:
- hidden controls;
- deterministic scorer/self-test;
- this authoring audit;
- prior Experiment 023 evidence/results;
- repository/agent routing instructions.

The hidden controls do **not** prescribe whether the final discovery status is SUPPORTED, PARTIAL, NOT_SUPPORTED, or INCOMPLETE.

They enforce only:
- all six modules considered;
- source-local relation distinctions preserved;
- surface shape not treated as proof;
- at least one falsifier;
- source specifications not refactored during discovery;
- candidate commonality not promoted to Core by the discovery run itself.

The scorer self-test accepts a methodologically valid synthetic PARTIAL report and rejects a report that treats shared surface shape as proof.
