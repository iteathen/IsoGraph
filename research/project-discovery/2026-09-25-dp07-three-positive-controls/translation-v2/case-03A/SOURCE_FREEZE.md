# v2 source freeze — case 03A

Continuous time domain and real-valued functions q(t),v(t),a(t).

Positive real parameters m,k.

```text
v(t) = D(q,t)
a(t) = D(v,t)
m*a(t) + k*q(t) = 0
```

D is the time-derivative operator under the frozen source interpretation.
