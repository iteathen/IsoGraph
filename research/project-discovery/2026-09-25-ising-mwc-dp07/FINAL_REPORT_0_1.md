# Project Discovery Ising / MWC — Final report 0.1

**Status:** COMPLETE first-pass exploratory comparison  
**Branch:** `experiment/dp07-ising-mwc-v1`  
**Authority effect:** none

## Executive result

DP 0.7 behaved as intended.

It found a real cross-domain structural common core without erasing the most important difference between the models.

### Common core

```text
repeated local binary variables
    ->
global state score
    ->
exponential weight
    ->
partition normalization
    ->
probability-weighted observable
```

An exact affine map relates the two local binary encodings:

```text
{-1,+1} <-> {0,1}
```

and exposes a second exact role correspondence:

```text
external field h
ligand chemical potential/control mu
```

both enter as linear biases on an aggregate local binary statistic, up to the required affine scaling/constants.

### Residual

The interaction mechanisms are not isomorphic.

```text
Ising:
    direct nearest-neighbor pair interaction on a cycle

MWC:
    one global binary conformation modulates all local binding contributions
```

The common statistical-mechanics layer therefore coexists with a load-bearing interaction-topology mismatch.

## Why this is a useful DP 0.7 test

A high-level comparison could easily have failed in either direction:

```text
different scientific vocabulary
    -> false negative

both show collective/cooperative response
    -> false positive
```

Primitive-first comparison avoided both.

It found:

- lower structural sameness;
- middle-layer difference;
- higher derived common behavior class without using that class as evidence.

## Strongest next lead

Eliminate the MWC global conformation exactly and derive the induced occupancy-only statistical score.

Then compare the induced interaction structure to the Ising pair interaction.

This is a high-value test for the combined Core 0.19 + DP 0.7 work:

1. Core 0.19 can expose the exact implicit assertions generated by marginalization/factorization.
2. DP 0.7 can compare the resulting primitive interaction structure without forcing a preselected label.

No claim is made yet about what interaction order will emerge.

## Development disposition

```text
candidate pair selection:                useful
independent source freeze:               complete
independent native rendering:            complete
DP 0.7 pass:                             complete
partial structural correspondence:       established
whole-system isomorphism:                rejected
next deeper comparison:                  justified
```
