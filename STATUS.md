# IsoGraph Status

**Project state:** research incubation / qualification corpus frozen; isolated execution pending  
**Repository:** `iteathen/IsoGraph`  
**Default branch:** `main`

## Current authority

Current semantic authority for new work is self-contained in:

- `CORE_SPEC_DRAFT_0_15_CONSOLIDATED_CANDIDATE.md`

It consolidates the surviving semantics of Drafts 0.13, 0.14, and 0.15 without adding a domain operator, structural class, comparison relation kind, or irreducible primitive. The source specifications remain historical provenance and rationale rather than required replay material for a current decoder.

## Qualification state

Experiment 004 construction is complete for the first current-authority qualification checkpoint:

- original Q004-01..Q004-20 target identities remain frozen;
- current Draft 0.15 prompt successors exist for all reusable original target run groups;
- Q004-17B adds separate current native-record pressure without rewriting Q004-17;
- supplemental Q004-S01..Q004-S08 each have native fixtures, hidden scorer assertions, and isolated cold prompts;
- `COLD_RUN_MANIFEST_004_V3.md` is the current execution manifest;
- new native-text fixtures use `.isg`; historical `.axh` files are preserved;
- **zero current-authority isolated cold runs have been accepted yet**.

IsoGraph therefore does **not** yet establish that the current representation is independently qualified, that its structural-only discovery path achieves useful corpus-scale recall, or that any candidate structural class is promoted.

## Immediate work

1. Dispatch Q004-S01/S02/S03/S06/S07 in genuinely fresh isolated contexts.
2. Freeze each raw report before opening its scorer assertions.
3. Dispatch Q004-S04/S05/S08, the reusable original-target groups, and Q004-17B under the same isolation rule.
4. Score only after output freeze and classify every discrepancy before specification repair.
5. Independently verify positive mappings/certificates and distinguish certified negatives from incomplete-search outcomes.
6. Only after this checkpoint consider further specification mutation or larger unlabeled cross-domain discovery tests.

The context that constructed the corpus has seen hidden assertions and is explicitly ineligible to produce accepted cold outputs.

## Naming and provenance

Current repository artifacts use **IsoGraph** terminology. The project was extracted from an earlier CUDA-JS research line; the exact pre-standalone state remains recoverable from the immutable source checkpoint recorded in `MIGRATION.md` and from Git history.

Legacy source identifiers are provenance only and do not define current project terminology.
