# Experiment 018 — Connect4 Game-Theory 1.2 Final Package Holdout

Use the frozen Connect4 package exactly as supplied.

## P01 — One package, not overlay composition

The candidate names a Markdown root, JSON graph, native graph, and claim-coverage companion.

Determine whether these are one semantic authority candidate or independent parallel authorities, and whether the historical NEI/RBA overlays remain active semantic layers.

## P02 — Claim closure with open claims preserved

The coverage companion reports 74 authority-1.1 claims plus 19 post-1.1 claims.

Determine the total covered claim count, uncovered count, and whether the existence of explicitly OPEN claims means those claims may be treated as solved by package qualification.

## P03 — Orientation-sensitive q_o identity

Two legal nonterminal standard-7x6 states have equal q_o.

Under the qualified q-congruence result and qualified NEI 0.4, determine whether their orientation-sensitive literal-action-labelled ordinary future behavior is SAME, and whether a Bayes factor is required.

## P04 — Reflection quotient q_r is not literal-label identity

Two states have equal q_r but opposite horizontal orientations.

Determine the exact action transporter and whether equal q_r alone authorizes identical literal column labels.

## P05 — q_r scalar value reuse versus proof identity

Two states have equal q_r.

Determine whether exact scalar W/D/L/value-cache reuse is supported and whether non-q proof/certificate identity follows.

## P06 — Physical/history identity remains finer than q_o

Two physically distinct legal positions have equal q_o.

Determine whether q_o future-behavior identity implies the same colored board, the same move history, or the same physical occurrence.

## P07 — Evidence independence QU is not identity UNKNOWN

QU-GAME-01 says evidence-lineage identity structure is known but cross-lineage statistical independence may be unresolved.

Determine whether this uncertainty is an identity question, whether it should be represented in QU, and whether unresolved independence alone establishes NEI UNKNOWN for the evidence artifacts.

## P08 — Rank-35 wall and missing law are different facts

C4-R0075 identifies distributed universal proof composition before semantic collapse as the observed rank-35 wall.

C4-R0076 is the compact realizability-preserving clause/proof -> q/value controllable-predecessor law.

Determine whether R0075 already supplies R0076, and classify R0076's status.

## P09 — RBA exact value does not become proof identity

An RBA relation closes an exact ordinary W/D/L consequence for a support/residual boundary.

Determine whether that exact value automatically transports deadline/resource/realizability/provenance proof premises or establishes proof/certificate identity.

## P10 — No invented Bayesian identity weight

The package has bounded q controls, exact q_o qualification, repeated replay checks, and solved-game evidence, but no qualified likelihood model for a proposed new identity-evidence statistic.

Determine whether a numerical Bayes factor may be invented from the number of passing controls.

## P11 — Historical-only claims remain evidence, not active primitives

The coverage companion marks some prior implementation/performance claims HISTORICAL_ONLY.

Determine whether they disappear from provenance, whether they become active game-theory primitives, and whether the package can still be complete while retaining them only as evidence.

## P12 — Solver schedules do not create new game semantics

IsoMax is described as demand-driven and BSFP as supply-driven over the ordinary value dependency.

Determine whether they own different game-theory value semantics or different evaluation/materialization schedules over the same semantic dependency.

## P13 — Hot-loop authority has no gameplay-authority effect

The qualified hot-loop 0.3 authority consumes q_r for cache/value behavior.

Determine whether hot-loop qualification can redefine q_o/q_r game semantics or whether those semantics remain owned by the game-theory package.

## P14 — Reflection equivalence versus physical occurrence

A position and its horizontal reflection are different physical occurrences.

Determine whether horizontal reflection supplies an exact automorphism/orbit equivalence and whether it makes the two physical occurrences one physical occurrence.

## P15 — Discovery does not promote candidate claims

The corrected DP-01..DP-45 rerun found and repaired the q_o/q_r distinction and reports no further semantic defect.

Determine whether a DP PASS automatically promotes open/candidate theorems to exact authority.

## P16 — Replacement semantics without split authority

Assume the 1.2 package passes final qualification and is promoted.

Determine whether authority 1.1 and the old applied-NEI/RBA overlays should remain co-equal active semantics, or become historical/provenance while 1.2 becomes the current game-theory authority.
