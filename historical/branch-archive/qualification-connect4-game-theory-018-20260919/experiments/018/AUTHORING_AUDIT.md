# Experiment 018 — Authoring Audit

**Status:** pre-trigger deterministic audit

## Leakage

Public cases do not contain expected answer values.

Public prompt exposes only key names, booleans/numeric types and enums, explicitly as serializer shapes.

Hidden assertions remain only in `experiments/018/hidden/ASSERTIONS.json`.

Cold runner forbids hidden/scorer/audit/review/evidence paths.

## Source freeze

The source manifest pins eight Connect4 files from:

`research/semantic-quotient@4f4e25b2b6fe10a11ee3145df2422b5df5fbc67f`.

The package includes:
- semantic Markdown root;
- machine JSON;
- native topology;
- 93-claim coverage companion;
- final q_o/q_r qualification;
- corrected DP 0.2 record;
- qualified hot-loop authority pointer.

## Scorer audit

The scorer checks exact answer keys/values and package self-assessment.

The scorer self-test:
1. requires a perfect synthetic report to QUALIFY;
2. changes P04 to overclaim literal action-label identity under q_r;
3. requires that report to DOES_NOT_QUALIFY.

## Scope

This audit validates qualification mechanics only. It is not semantic evidence for the package.
