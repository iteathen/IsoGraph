name: Experiment 018 DP 0.6 qualification
on:
  push:
    branches: [qualification/dp-0-6-018-20260925]
    paths: [experiments/018/RUN.request]
permissions:
  contents: write
concurrency:
  group: experiment-018-dp-0-6
  cancel-in-progress: false
env:
  GEMINI_MODEL: gemini-3.5-flash
jobs:
  qualify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with: {fetch-depth: 0}
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with: {node-version: 26.7.0}
      - name: Deterministic preflight
        run: |
          set -euo pipefail
          node --check experiments/018/tools/run-exp018-cold.mjs
          node --check experiments/018/tools/score-exp018.mjs
          node experiments/018/tools/test-score-exp018.mjs
          rm -rf out/exp018
          ISOGRAPH_COLD_DRY_RUN=1 node experiments/018/tools/run-exp018-cold.mjs
      - name: Execute isolated semantic call
        id: cold
        continue-on-error: true
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node experiments/018/tools/run-exp018-cold.mjs
      - name: Score
        id: score
        if: always() && steps.cold.outcome == 'success'
        continue-on-error: true
        run: |
          set -euo pipefail
          dry=$(node -e "const fs=require('fs');console.log(JSON.parse(fs.readFileSync('out/exp018/DRY_RUN.json','utf8')).packet_sha256)")
          live=$(node -e "const fs=require('fs');console.log(JSON.parse(fs.readFileSync('out/exp018/METADATA.json','utf8')).packet_sha256)")
          test "$dry" = "$live"
          node experiments/018/tools/score-exp018.mjs
      - name: Persist evidence
        if: always()
        env:
          COLD_OUTCOME: ${{ steps.cold.outcome }}
          SCORE_OUTCOME: ${{ steps.score.outcome }}
        run: |
          set -euo pipefail
          E="experiments/018/evidence/run-${GITHUB_RUN_ID}-attempt-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$E"
          for f in DRY_RUN.json METADATA.json COLD_REPORT_RAW.txt PARSED_REPORT.json SCORE.json; do [ -f "out/exp018/$f" ] && cp "out/exp018/$f" "$E/$f"; done
          printf '{"workflow_run_id":"%s","workflow_attempt":"%s","qualification_sha":"%s","cold_outcome":"%s","score_outcome":"%s"}\n' "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$GITHUB_SHA" "$COLD_OUTCOME" "$SCORE_OUTCOME" > "$E/CONTROL.json"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$E"
          if ! git diff --cached --quiet; then git commit -m "evidence: Experiment 018 run ${GITHUB_RUN_ID} attempt ${GITHUB_RUN_ATTEMPT}"; git push origin HEAD:qualification/dp-0-6-018-20260925; fi
      - name: Enforce qualification
        if: always()
        env:
          COLD_OUTCOME: ${{ steps.cold.outcome }}
        run: |
          set -euo pipefail
          test "$COLD_OUTCOME" = "success"
          test -f out/exp018/SCORE.json
          node -e "const fs=require('fs');const s=JSON.parse(fs.readFileSync('out/exp018/SCORE.json','utf8'));if(s.disposition!=='QUALIFIES')process.exit(1)"
