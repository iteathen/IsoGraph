name: Experiment 013 focused QU NEI qualification

on:
  push:
    branches:
      - qualification/module-campaign-013-20260918
    paths:
      - experiments/013/RUN.request

permissions:
  contents: write

concurrency:
  group: experiment-013-qu-nei-focused
  cancel-in-progress: false

env:
  GEMINI_MODEL: gemini-3.5-flash

jobs:
  qualify:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out exact qualification revision
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.7.0

      - name: Deterministic preflight
        shell: bash
        run: |
          set -euo pipefail
          node --check experiments/013/tools/run-exp013-cold.mjs
          node --check experiments/013/tools/score-exp013.mjs
          node --check experiments/013/tools/test-score-exp013.mjs
          node experiments/013/tools/test-score-exp013.mjs
          rm -rf out/exp013
          ISOGRAPH_COLD_DRY_RUN=1 node experiments/013/tools/run-exp013-cold.mjs
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const d=JSON.parse(fs.readFileSync('out/exp013/DRY_RUN.json','utf8'));
          const paths=d.input_manifest.map(x=>x.path);
          for(const f of ['hidden/','ASSERTIONS','score-exp013','AUTHORING_AUDIT','experiments/009/evidence']){
            if(paths.some(p=>p.includes(f)))throw new Error('forbidden cold input '+f);
          }
          console.log('dry_packet_sha256='+d.packet_sha256);
          NODE

      - name: Execute isolated semantic call
        id: cold
        continue-on-error: true
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node experiments/013/tools/run-exp013-cold.mjs

      - name: Verify packet identity and score
        id: score
        if: always() && steps.cold.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          dry=$(node -e "const fs=require('fs');console.log(JSON.parse(fs.readFileSync('out/exp013/DRY_RUN.json','utf8')).packet_sha256)")
          live=$(node -e "const fs=require('fs');console.log(JSON.parse(fs.readFileSync('out/exp013/METADATA.json','utf8')).packet_sha256)")
          test "$dry" = "$live"
          node experiments/013/tools/score-exp013.mjs

      - name: Persist sanitized evidence to branch
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          E="experiments/013/evidence/run-${GITHUB_RUN_ID}"
          mkdir -p "$E"
          for f in DRY_RUN.json METADATA.json COLD_REPORT_RAW.txt PARSED_REPORT.json SCORE.json; do
            [ -f "out/exp013/$f" ] && cp "out/exp013/$f" "$E/$f"
          done
          cat > "$E/CONTROL.json" <<EOF
          {
            "workflow_run_id": "${GITHUB_RUN_ID}",
            "workflow_attempt": "${GITHUB_RUN_ATTEMPT}",
            "qualification_sha": "${GITHUB_SHA}",
            "cold_outcome": "${{ steps.cold.outcome }}",
            "score_outcome": "${{ steps.score.outcome }}"
          }
          EOF
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$E"
          if ! git diff --cached --quiet; then
            git commit -m "evidence: Experiment 013 run ${GITHUB_RUN_ID}"
            git push origin HEAD:qualification/module-campaign-013-20260918
          fi

      - name: Enforce qualification
        if: always()
        env:
          COLD_OUTCOME: ${{ steps.cold.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          test "$COLD_OUTCOME" = "success"
          test -f out/exp013/SCORE.json
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const s=JSON.parse(fs.readFileSync('out/exp013/SCORE.json','utf8'));
          console.log(JSON.stringify({disposition:s.disposition,qu:s.qu_0_1,nei:s.nei_0_1_0_2},null,2));
          if(s.disposition!=='QUALIFIES')process.exit(1);
          NODE
