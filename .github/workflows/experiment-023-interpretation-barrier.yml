name: Experiment 023 interpretation-barrier discovery

on:
  push:
    branches:
      - research/dts-0.1
    paths:
      - experiments/023/RUN.request

permissions:
  contents: write

concurrency:
  group: experiment-023-interpretation-barrier
  cancel-in-progress: false

env:
  GEMINI_MODEL: gemini-2.5-pro

jobs:
  discover:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 26.7.0

      - name: Deterministic preflight
        shell: bash
        run: |
          set -euo pipefail
          node --check experiments/023/tools/run-exp023-cold.mjs
          node --check experiments/023/tools/score-exp023.mjs
          node --check experiments/023/tools/test-score-exp023.mjs
          node experiments/023/test-barriers.mjs
          node experiments/023/tools/test-score-exp023.mjs
          rm -rf out/exp023
          ISOGRAPH_COLD_DRY_RUN=1 node experiments/023/tools/run-exp023-cold.mjs
          node -e "const fs=require('fs');const d=JSON.parse(fs.readFileSync('out/exp023/DRY_RUN.json','utf8'));const p=d.input_manifest.map(x=>x.path);const bad=['hidden/','CONTROLS','score-exp023','AUTHORING_AUDIT','RESULTS','experiments/023/evidence','AGENTS.md','STATUS.md'];for(const f of bad)if(p.some(x=>x.includes(f)))throw new Error('forbidden cold input '+f);console.log('dry_packet_sha256='+d.packet_sha256)"

      - name: Execute isolated discovery call
        id: cold
        continue-on-error: true
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node experiments/023/tools/run-exp023-cold.mjs

      - name: Verify packet identity and score
        id: score
        if: always() && steps.cold.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          dry=$(node -e "const fs=require('fs');console.log(JSON.parse(fs.readFileSync('out/exp023/DRY_RUN.json','utf8')).packet_sha256)")
          live=$(node -e "const fs=require('fs');console.log(JSON.parse(fs.readFileSync('out/exp023/METADATA.json','utf8')).packet_sha256)")
          test "$dry" = "$live"
          node experiments/023/tools/score-exp023.mjs

      - name: Persist evidence
        if: always()
        env:
          COLD_OUTCOME: ${{ steps.cold.outcome }}
          SCORE_OUTCOME: ${{ steps.score.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          E="experiments/023/evidence/run-${GITHUB_RUN_ID}-attempt-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "$E"
          for f in DRY_RUN.json METADATA.json COLD_REPORT_RAW.txt PARSED_REPORT.json SCORE.json; do
            [ -f "out/exp023/$f" ] && cp "out/exp023/$f" "$E/$f"
          done
          printf '{"workflow_run_id":"%s","workflow_attempt":"%s","source_sha":"%s","cold_outcome":"%s","score_outcome":"%s"}\n' "$GITHUB_RUN_ID" "$GITHUB_RUN_ATTEMPT" "$GITHUB_SHA" "$COLD_OUTCOME" "$SCORE_OUTCOME" > "$E/CONTROL.json"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$E"
          if ! git diff --cached --quiet; then
            git commit -m "evidence: Experiment 023 run ${GITHUB_RUN_ID} attempt ${GITHUB_RUN_ATTEMPT}"
            git push origin HEAD:research/dts-0.1
          fi

      - name: Enforce discovery methodology controls
        if: always()
        env:
          COLD_OUTCOME: ${{ steps.cold.outcome }}
        shell: bash
        run: |
          set -euo pipefail
          test "$COLD_OUTCOME" = "success"
          test -f out/exp023/SCORE.json
          node -e "const fs=require('fs');const s=JSON.parse(fs.readFileSync('out/exp023/SCORE.json','utf8'));console.log(JSON.stringify(s,null,2));if(s.disposition!=='PASSES')process.exit(1)"
