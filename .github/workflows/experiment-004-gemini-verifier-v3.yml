name: Experiment 004 Gemini Independent Verifier V3

on:
  push:
    branches:
      - experiment/004-gemini-results
    paths:
      - .github/workflows/experiment-004-gemini-verifier-v3.yml

permissions:
  contents: write

concurrency:
  group: experiment-004-gemini-verifier-2-5
  cancel-in-progress: true

env:
  GEMINI_MODEL: gemini-2.5-flash

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify Gemini key exists
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        shell: bash
        run: test -n "${GEMINI_API_KEY:-}"

  verify:
    needs: preflight
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        run: [RUN-S05, RUN-S08, RUN-B, RUN-C1, RUN-C2, RUN-D09, RUN-D10, RUN-D16, RUN-D18, RUN-D19, RUN-D20S, RUN-D20L]
    steps:
      - name: Check out evidence history
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
          ref: experiment/004-gemini-results

      - name: Fetch Claude evidence branch
        shell: bash
        run: git fetch origin experiment/004-cold-results-claude:refs/remotes/origin/experiment/004-cold-results-claude

      - name: Select Gemini 2.5 thinking control
        shell: bash
        run: |
          set -euo pipefail
          sed -i "s/thinkingConfig: { thinkingLevel: 'HIGH' }/thinkingConfig: { thinkingBudget: 24576 }/" experiments/004/tools/run-gemini-verifier.mjs
          grep -F "thinkingBudget: 24576" experiments/004/tools/run-gemini-verifier.mjs

      - name: Execute scorer-blind structural verifier
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          RUN_ID: ${{ matrix.run }}
        run: node experiments/004/tools/run-gemini-verifier.mjs

      - name: Freeze verifier artifact
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: isograph-exp004-${{ matrix.run }}-verifier-v3
          path: out/
          if-no-files-found: error
          retention-days: 30

  archive:
    if: always()
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out latest results branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
          ref: experiment/004-gemini-results

      - name: Download V3 verifier artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: isograph-exp004-*-verifier-v3
          path: verifier-downloads
          merge-multiple: true

      - name: Archive V3 verifier outputs
        shell: bash
        run: |
          set -euo pipefail
          dest=experiments/004/cold-results/gemini/verifier
          mkdir -p "$dest"
          found=0
          for report in verifier-downloads/*_VERIFIER_REPORT.txt; do
            [ -e "$report" ] || continue
            base="$(basename "$report" _VERIFIER_REPORT.txt)"
            mkdir -p "$dest/$base"
            cp "$report" "$dest/$base/VERIFIER_REPORT.txt"
            cp "verifier-downloads/${base}_VERIFIER_METADATA.json" "$dest/$base/VERIFIER_METADATA.json"
            found=$((found+1))
          done
          printf '# Experiment 004 — scorer-blind verifier archive\n\nV3 workflow run: `%s`\n\nV3 successful reports: **%s / 12**.\n\nV1 successful reports for RUN-A, RUN-S01, RUN-S02, RUN-S03, RUN-S06, RUN-S07, and RUN-S04 remain separately frozen in Actions artifacts and are not overwritten here.\n\nVerifier packets exclude hidden assertions, scoring reports, qualification ledgers, expected answers, and prior verifier outputs.\n' "$GITHUB_RUN_ID" "$found" > "$dest/MANIFEST.md"
          test "$found" -gt 0

      - name: Commit V3 verifier archive
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "iteathen"
          git config user.email "josh.oshiro@gmail.com"
          git add experiments/004/cold-results/gemini/verifier
          if git diff --cached --quiet; then exit 0; fi
          git diff --cached --check
          git commit -m 'experiment: archive Gemini 2.5 scorer-blind verifier evidence'
          git push origin HEAD:experiment/004-gemini-results
